apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-configmap
data:
  MYSQL_HOST: host.minikube.internal
  MYSQL_USER: auth_user
  MYSQL_DB: auth
  MYSQL_PORT: "3306"
  JWT_ISSUER: "video-to-audio-auth"
  JWT_AUDIENCE: "video-to-audio-api"
  JWT_TTL_SECONDS: "3600"
